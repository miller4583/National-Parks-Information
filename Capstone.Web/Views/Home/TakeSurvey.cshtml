@using Capstone.Web.Models;
@model Survey
@{
    ViewBag.Title = "TakeSurvey";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 class="text-center title-head pad">Park Survey</h2>

<form action="@Url.Action("TakeSurvey","Home")" class="text-center" method="post">
    <div >
        <div class="form-group ">
            @Html.LabelFor(m => m.ParkCode)
            <span class="input-group center-block">@Html.DropDownListFor(m => m.ParkCode, ViewBag.ParkCodes as List<SelectListItem>)</span>
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.Email)
            <span class="input-group center-block">
                @Html.TextBoxFor(m => m.Email)
            </span>
            @Html.ValidationMessageFor(m => m.Email)
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.State)
            <span class="input-group center-block">
                @Html.TextBoxFor(m => m.State)
            </span>
            @Html.ValidationMessageFor(m => m.State)
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.ActivityLevel)
            <span class="input-group center-block">
                @Html.TextAreaFor(m => m.ActivityLevel)
            </span>
            @Html.ValidationMessageFor(m => m.ActivityLevel)
        </div>
    </div>
    <div class="text-center pad">
        <button>Submit Survey</button>
    </div>
</form>

